import Section from '@/components/Section/Section';

const Home = () => (
  <div className="flex items-center justify-center bg-white font-sans light:bg-white">
    <main className="flex w-full max-w-full flex-col items-center justify-center py-10 px-16 bg-white light:bg-black ">
      <Section />
    </main>
  </div>
);

export default Home;
